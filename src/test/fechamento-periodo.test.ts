import{describe,expect,it}from"vitest";import fs from"node:fs";import path from"node:path";const raiz=path.resolve(__dirname,"../..");const ler=(a:string)=>fs.readFileSync(path.join(raiz,a),"utf8");describe("fechamento financeiro",()=>{it("bloqueia a data de fechamento e todas as anteriores",()=>{const s=ler("backend/src/services/PeriodoFinanceiroService.ts");expect(s).toContain("data<=r.fechado_ate");expect(s).toContain("Período financeiro fechado até")});it("protege lançamentos, recebimentos e pagamentos",()=>{for(const a of["backend/src/routes/modules/lancamentos.ts","backend/src/routes/modules/pagamentos.ts","backend/src/routes/modules/despesas.ts"])expect(ler(a)).toContain("verificarPeriodoFinanceiro")});it("não cria fechamento padrão e oferece confirmação",()=>{const m=ler("backend/src/migrations/1700000000043-CreateFechamentosFinanceiros.ts");const t=ler("src/components/financeiro/FechamentoPeriodoTab.tsx");expect(m).not.toMatch(/INSERT INTO fechamentos_financeiros/i);expect(t).toContain("DestructiveConfirmationDialog");expect(t).toContain("Registros históricos não serão alterados")})});
