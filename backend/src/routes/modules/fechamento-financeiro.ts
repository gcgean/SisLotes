import { Router, Response } from "express";
import { z } from "zod";
import { AppDataSource } from "../../db/data-source";
import { AuthRequest, requireAuth, requireFeature } from "../../middleware/auth";
import { AuditoriaService } from "../../services/AuditoriaService";

export const fechamentoFinanceiroRouter = Router();
fechamentoFinanceiroRouter.use(requireAuth, requireFeature("module_despesas"));

const periodoSchema=z.object({from:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),to:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),id_conta:z.coerce.number().int().positive().optional()}).refine(({from,to})=>from<=to,{message:"A data inicial deve ser anterior ou igual à data final"});
type QueryExecutor={query:(sql:string,params?:unknown[])=>Promise<unknown[]>};

async function validarConta(db:QueryExecutor,empresa:number,idConta?:number|null){if(!idConta)return null;const[conta]=await db.query(`SELECT id_conta,apelido FROM contas WHERE id_empresa=$1 AND id_conta=$2`,[empresa,idConta]) as Array<{id_conta:number;apelido:string}>;return conta??null;}
async function obterResumo(db:QueryExecutor,empresa:number,from:string,to:string,idConta?:number|null){const[r]=await db.query(`WITH saldo_base AS (SELECT COALESCE(SUM(c.saldo_inicial),0)::numeric valor FROM contas c WHERE c.id_empresa=$1 AND ($4::int IS NULL OR c.id_conta=$4)),movimentos_anteriores AS (SELECT COALESCE(SUM(CASE WHEN mf.tipo='receita' THEN mf.valor ELSE -mf.valor END),0)::numeric valor FROM movimentos_financeiros mf JOIN contas c ON c.id_conta=mf.id_conta AND c.id_empresa=$1 WHERE mf.id_empresa=$1 AND ($4::int IS NULL OR mf.id_conta=$4) AND mf.data<$2::date AND mf.data>=COALESCE(c.data_saldo_inicial,'1900-01-01'::date)),realizado AS (SELECT COALESCE(SUM(mf.valor) FILTER(WHERE mf.tipo='receita'),0)::numeric entradas,COALESCE(SUM(mf.valor) FILTER(WHERE mf.tipo='despesa'),0)::numeric saidas FROM movimentos_financeiros mf WHERE mf.id_empresa=$1 AND ($4::int IS NULL OR mf.id_conta=$4) AND mf.data BETWEEN $2::date AND $3::date),receber_pendente AS (SELECT COALESCE(SUM(p.valor),0)::numeric valor,COUNT(*)::int quantidade FROM pagamentos p JOIN vendas v ON v.id_venda=p.id_venda WHERE $4::int IS NULL AND p.id_empresa=$1 AND p.situacao='aberto' AND v.status<>'cancelada' AND p.vencimento BETWEEN $2::date AND $3::date),pagar_pendente AS (SELECT COALESCE(SUM(GREATEST(dp.valor-COALESCE(b.liquidado,0),0)),0)::numeric valor,COUNT(*)::int quantidade FROM despesa_parcelas dp LEFT JOIN LATERAL(SELECT COALESCE(SUM(pp.valor_principal+pp.desconto),0)::numeric liquidado FROM despesa_parcela_pagamentos pp WHERE pp.id_despesa_parcela=dp.id_despesa_parcela AND pp.id_empresa=$1)b ON true WHERE $4::int IS NULL AND dp.id_empresa=$1 AND dp.situacao<>'pago' AND dp.vencimento BETWEEN $2::date AND $3::date) SELECT saldo_base.valor+movimentos_anteriores.valor saldo_inicial,realizado.entradas,realizado.saidas,saldo_base.valor+movimentos_anteriores.valor+realizado.entradas-realizado.saidas saldo_final,realizado.entradas-realizado.saidas variacao_caixa,receber_pendente.valor contas_receber_pendentes,receber_pendente.quantidade qtd_receber_pendentes,pagar_pendente.valor contas_pagar_pendentes,pagar_pendente.quantidade qtd_pagar_pendentes,receber_pendente.valor-pagar_pendente.valor saldo_nao_consolidado FROM saldo_base,movimentos_anteriores,realizado,receber_pendente,pagar_pendente`,[empresa,from,to,idConta??null]) as Record<string,unknown>[];return{periodo:{from,to},idConta:idConta??null,saldoInicial:Number(r.saldo_inicial),entradas:Number(r.entradas),saidas:Number(r.saidas),saldoFinal:Number(r.saldo_final),variacaoCaixa:Number(r.variacao_caixa),contasReceberPendentes:Number(r.contas_receber_pendentes),qtdReceberPendentes:Number(r.qtd_receber_pendentes),contasPagarPendentes:Number(r.contas_pagar_pendentes),qtdPagarPendentes:Number(r.qtd_pagar_pendentes),saldoNaoConsolidado:Number(r.saldo_nao_consolidado)}}
async function obterMovimentos(db:QueryExecutor,empresa:number,from:string,to:string,idConta?:number|null){const rows=await db.query(`SELECT TO_CHAR(mf.data,'YYYY-MM-DD') data,mf.tipo,mf.descricao,c.apelido conta,mf.origem,mf.id_origem,COALESCE(l.nome,'Administrativa') loteamento,mf.valor::numeric valor FROM movimentos_financeiros mf JOIN contas c ON c.id_conta=mf.id_conta LEFT JOIN loteamentos l ON l.id_loteamento=mf.id_loteamento AND l.id_empresa=$1 WHERE mf.id_empresa=$1 AND ($4::int IS NULL OR mf.id_conta=$4) AND mf.data BETWEEN $2::date AND $3::date ORDER BY mf.data,mf.tipo,mf.id_origem`,[empresa,from,to,idConta??null]) as Array<Record<string,unknown>>;return rows.map(m=>({...m,valor:Number(m.valor)}))}

fechamentoFinanceiroRouter.get("/",async(req:AuthRequest,res:Response)=>{const rows=await AppDataSource.query(`SELECT f.id_fechamento::int id_fechamento,f.id_conta,COALESCE(c.apelido,'Todas as contas') conta_nome,TO_CHAR(f.fechado_ate,'YYYY-MM-DD') fechado_ate,f.updated_at,(SELECT rf.id_relatorio::int FROM relatorios_fechamento_financeiro rf WHERE rf.id_empresa=f.id_empresa AND rf.status='fechado' AND rf.id_conta IS NOT DISTINCT FROM f.id_conta ORDER BY rf.periodo_fim DESC,rf.id_relatorio DESC LIMIT 1) id_relatorio FROM fechamentos_financeiros f LEFT JOIN contas c ON c.id_conta=f.id_conta WHERE f.id_empresa=$1 AND f.fechado_ate IS NOT NULL ORDER BY f.id_conta NULLS FIRST,c.apelido`,[req.user!.id_empresa]);return res.json({fechamentos:rows})});
fechamentoFinanceiroRouter.get("/extrato",async(req:AuthRequest,res:Response)=>{const p=periodoSchema.safeParse(req.query);if(!p.success)return res.status(400).json({error:"Período inválido",issues:p.error.issues});if(p.data.id_conta&&!await validarConta(AppDataSource,req.user!.id_empresa,p.data.id_conta))return res.status(404).json({error:"Conta não encontrada"});const[resumo,movimentos]=await Promise.all([obterResumo(AppDataSource,req.user!.id_empresa,p.data.from,p.data.to,p.data.id_conta),obterMovimentos(AppDataSource,req.user!.id_empresa,p.data.from,p.data.to,p.data.id_conta)]);return res.json({...resumo,movimentos})});
fechamentoFinanceiroRouter.get("/relatorios",async(req:AuthRequest,res:Response)=>res.json(await AppDataSource.query(`SELECT rf.id_relatorio::int id_relatorio,rf.id_conta,COALESCE(rf.conta_nome,'Todas as contas') conta_nome,TO_CHAR(rf.periodo_inicio,'YYYY-MM-DD') periodo_inicio,TO_CHAR(rf.periodo_fim,'YYYY-MM-DD') periodo_fim,rf.status,rf.resumo,rf.fechado_em,rf.desfeito_em,u.login fechado_por_login,ud.login desfeito_por_login,jsonb_array_length(rf.lancamentos) qtd_lancamentos FROM relatorios_fechamento_financeiro rf JOIN usuarios u ON u.id_usuario=rf.fechado_por LEFT JOIN usuarios ud ON ud.id_usuario=rf.desfeito_por WHERE rf.id_empresa=$1 ORDER BY rf.periodo_fim DESC,rf.id_relatorio DESC`,[req.user!.id_empresa])));
fechamentoFinanceiroRouter.get("/relatorios/:id",async(req:AuthRequest,res:Response)=>{const[row]=await AppDataSource.query(`SELECT rf.*,rf.id_relatorio::int id_relatorio,COALESCE(rf.conta_nome,'Todas as contas') conta_nome,TO_CHAR(rf.periodo_inicio,'YYYY-MM-DD') periodo_inicio,TO_CHAR(rf.periodo_fim,'YYYY-MM-DD') periodo_fim,u.login fechado_por_login,ud.login desfeito_por_login,e.nome_fantasia empresa_nome FROM relatorios_fechamento_financeiro rf JOIN usuarios u ON u.id_usuario=rf.fechado_por JOIN empresas e ON e.id_empresa=rf.id_empresa LEFT JOIN usuarios ud ON ud.id_usuario=rf.desfeito_por WHERE rf.id_relatorio=$1 AND rf.id_empresa=$2`,[Number(req.params.id),req.user!.id_empresa]);return row?res.json(row):res.status(404).json({error:"Relatório de fechamento não encontrado"})});

fechamentoFinanceiroRouter.put("/",async(req:AuthRequest,res:Response)=>{const parsed=z.object({fechado_ate:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),id_conta:z.number().int().positive().nullable().optional()}).safeParse(req.body);if(!parsed.success)return res.status(400).json({error:"Dados inválidos"});const empresa=req.user!.id_empresa,usuario=req.user!.id_usuario,fim=parsed.data.fechado_ate,idConta=parsed.data.id_conta??null;const runner=AppDataSource.createQueryRunner();await runner.connect();await runner.startTransaction();try{const conta=await validarConta(runner,empresa,idConta);if(idConta&&!conta){await runner.rollbackTransaction();return res.status(404).json({error:"Conta não encontrada"})}const[atual]=await runner.query(`SELECT id_fechamento,TO_CHAR(fechado_ate,'YYYY-MM-DD') fechado_ate FROM fechamentos_financeiros WHERE id_empresa=$1 AND id_conta IS NOT DISTINCT FROM $2::int FOR UPDATE`,[empresa,idConta]);if(atual?.fechado_ate&&fim<=atual.fechado_ate){await runner.rollbackTransaction();return res.status(409).json({error:`O novo fechamento deve ser posterior a ${atual.fechado_ate}. Para reabrir, use Desfazer fechamento.`})}const[inicioRow]=atual?.fechado_ate?await runner.query(`SELECT TO_CHAR(($1::date+INTERVAL '1 day')::date,'YYYY-MM-DD') inicio`,[atual.fechado_ate]):await runner.query(`SELECT COALESCE(TO_CHAR(MIN(data),'YYYY-MM-DD'),$2) inicio FROM movimentos_financeiros WHERE id_empresa=$1 AND ($3::int IS NULL OR id_conta=$3) AND data<=$2::date`,[empresa,fim,idConta]);const inicio=inicioRow.inicio as string;const[resumo,movimentos]=await Promise.all([obterResumo(runner,empresa,inicio,fim,idConta),obterMovimentos(runner,empresa,inicio,fim,idConta)]);const contaNome=conta?.apelido??"Todas as contas";const[relatorio]=await runner.query(`INSERT INTO relatorios_fechamento_financeiro(id_empresa,id_conta,conta_nome,periodo_inicio,periodo_fim,fechado_por,resumo,lancamentos) VALUES($1,$2,$3,$4,$5,$6,$7::jsonb,$8::jsonb) RETURNING id_relatorio::int`,[empresa,idConta,contaNome,inicio,fim,usuario,JSON.stringify(resumo),JSON.stringify(movimentos)]);let salvo;if(atual){[salvo]=await runner.query(`UPDATE fechamentos_financeiros SET fechado_ate=$2,id_usuario=$3,updated_at=now() WHERE id_fechamento=$1 RETURNING *`,[atual.id_fechamento,fim,usuario])}else{[salvo]=await runner.query(`INSERT INTO fechamentos_financeiros(id_empresa,id_conta,fechado_ate,id_usuario) VALUES($1,$2,$3,$4) RETURNING *`,[empresa,idConta,fim,usuario])}await runner.commitTransaction();await AuditoriaService.registrar(req,"fechamentos_financeiros",atual?"UPDATE":"CREATE",Number(salvo.id_fechamento),atual,salvo,`Fechamento de ${contaNome}: ${inicio} a ${fim} — relatório #${relatorio.id_relatorio}`);return res.json({...salvo,id_relatorio:relatorio.id_relatorio})}catch(error){await runner.rollbackTransaction();throw error}finally{await runner.release()}});

fechamentoFinanceiroRouter.post("/relatorios/:id/desfazer",async(req:AuthRequest,res:Response)=>{const empresa=req.user!.id_empresa,usuario=req.user!.id_usuario,id=Number(req.params.id),runner=AppDataSource.createQueryRunner();await runner.connect();await runner.startTransaction();try{const[relatorio]=await runner.query(`SELECT *,TO_CHAR(periodo_fim,'YYYY-MM-DD') periodo_fim FROM relatorios_fechamento_financeiro WHERE id_relatorio=$1 AND id_empresa=$2 FOR UPDATE`,[id,empresa]);if(!relatorio){await runner.rollbackTransaction();return res.status(404).json({error:"Relatório de fechamento não encontrado"})}if(relatorio.status==="desfeito"){await runner.rollbackTransaction();return res.status(409).json({error:"Este fechamento já foi desfeito"})}const[atual]=await runner.query(`SELECT id_fechamento,TO_CHAR(fechado_ate,'YYYY-MM-DD') fechado_ate FROM fechamentos_financeiros WHERE id_empresa=$1 AND id_conta IS NOT DISTINCT FROM $2::int FOR UPDATE`,[empresa,relatorio.id_conta]);if(atual?.fechado_ate!==relatorio.periodo_fim){await runner.rollbackTransaction();return res.status(409).json({error:"Somente o fechamento mais recente desta conta pode ser desfeito"})}await runner.query(`UPDATE relatorios_fechamento_financeiro SET status='desfeito',desfeito_por=$2,desfeito_em=now() WHERE id_relatorio=$1`,[id,usuario]);const[anterior]=await runner.query(`SELECT TO_CHAR(periodo_fim,'YYYY-MM-DD') periodo_fim FROM relatorios_fechamento_financeiro WHERE id_empresa=$1 AND status='fechado' AND id_relatorio<>$2 AND id_conta IS NOT DISTINCT FROM $3::int ORDER BY periodo_fim DESC,id_relatorio DESC LIMIT 1`,[empresa,id,relatorio.id_conta]);await runner.query(`UPDATE fechamentos_financeiros SET fechado_ate=$2,id_usuario=$3,updated_at=now() WHERE id_fechamento=$1`,[atual.id_fechamento,anterior?.periodo_fim??null,usuario]);await runner.commitTransaction();await AuditoriaService.registrar(req,"relatorios_fechamento_financeiro","UPDATE",id,relatorio,{status:"desfeito",fechado_ate:anterior?.periodo_fim??null},`Fechamento #${id} desfeito`);return res.json({fechado_ate:anterior?.periodo_fim??null})}catch(error){await runner.rollbackTransaction();throw error}finally{await runner.release()}});
