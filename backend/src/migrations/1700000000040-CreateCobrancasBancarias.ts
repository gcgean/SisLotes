import {MigrationInterface,QueryRunner} from "typeorm";
export class CreateCobrancasBancarias1700000000040 implements MigrationInterface{name="CreateCobrancasBancarias1700000000040";async up(q:QueryRunner){await q.query(`CREATE TABLE cobrancas_bancarias(id_cobranca SERIAL PRIMARY KEY,id_empresa integer NOT NULL,id_pagamento integer REFERENCES pagamentos(id_pagamento),id_conta integer REFERENCES contas(id_conta),tipo varchar(10) NOT NULL CHECK(tipo IN('boleto','pix')),status varchar(24) NOT NULL DEFAULT 'rascunho' CHECK(status IN('rascunho','aguardando_integracao','emitida','paga','cancelada','erro')),descricao varchar(300) NOT NULL,valor numeric(12,2) NOT NULL CHECK(valor>0),vencimento date NOT NULL,provedor varchar(80),id_externo varchar(150),nosso_numero varchar(80),linha_digitavel varchar(200),pix_copia_cola text,payload_provedor jsonb,erro_integracao text,id_usuario integer NOT NULL REFERENCES usuarios(id_usuario),created_at timestamp NOT NULL DEFAULT now(),updated_at timestamp NOT NULL DEFAULT now())`);await q.query(`CREATE INDEX idx_cobrancas_empresa_status_vencimento ON cobrancas_bancarias(id_empresa,status,vencimento)`);await q.query(`CREATE UNIQUE INDEX uq_cobrancas_provedor_externo ON cobrancas_bancarias(id_empresa,provedor,id_externo) WHERE id_externo IS NOT NULL`);}async down(q:QueryRunner){await q.query(`DROP TABLE cobrancas_bancarias`);}}
